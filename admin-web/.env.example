# Base URL of the existing Tuition backend API (must include the /api suffix).
# Local backend:      http://localhost:5000/api
# Production backend: https://hometuitionapp.com/api
VITE_API_URL=https://hometuitionapp.com/api

# Label shown in the sidebar footer. Purely cosmetic.
VITE_ENVIRONMENT=production
